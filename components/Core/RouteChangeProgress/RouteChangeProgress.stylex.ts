import * as stylex from "@stylexjs/stylex";

export const baseStyles = stylex.create({
    progressBar: {
        left: 0,
        position: "absolute",
        right: 0,
        top: 0,
        width: "100%",
        zIndex: 3,
    },
});
